# Система управления сделками автосалона - Инструкция по запуску

## ✅ Реализованная функциональность

Создана полнофункциональная система управления сделками для автосалона:

### Backend (Node.js + PostgreSQL)
- API endpoints для всех операций сделок
- JWT аутентификация
- Ролевая система доступа

### Frontend (Flutter)
- BLoC архитектура
- Полный пользовательский интерфейс
- Интеграция с существующей системой

## 🚀 Как запустить систему

### 1. Запуск Backend сервера
```bash
cd lib/backend
node real_server.js
```

Сервер запустится на http://localhost:3000

### 2. Установка зависимостей Flutter
```bash
flutter pub get
```

### 3. Запуск Flutter приложения
```bash
flutter run
```

### 4. Тестирование функциональности

#### Вход в систему
- Используйте существующие аккаунты с ролями admin или manager
- viewer роли могут только просматривать сделки

#### Доступ к сделкам
- После входа в главном экране нажмите иконку 📋 (assignment)
- Откроется экран управления сделками

#### Функции для admin/manager
- **Создание сделки**: Кнопка "+" в правом верхнем углу
- **Фильтрация**: Кнопка фильтра в AppBar
- **Просмотр деталей**: Нажатие на карточку сделки
- **Изменение статуса**: Через детальный просмотр
- **Удаление**: Только для роли admin

#### Статусы сделок
- 🟦 **Новая** (new) - только что созданная
- 🟠 **В процессе** (in_process) - активная сделка
- 🟢 **Завершена** (completed) - успешно завершенная
- 🔴 **Отменена** (canceled) - отмененная сделка

#### Типы сделок
- 🟣 **Продажа** (sale) - полная продажа автомобиля
- 🟢 **Бронирование** (reservation) - бронирование автомобиля

## 📋 API Endpoints

### Сделки
- `GET /api/deals` - Получить список сделок
- `GET /api/deals/:id` - Получить сделку по ID
- `POST /api/deals` - Создать новую сделку
- `PUT /api/deals/:id` - Обновить сделку
- `PUT /api/deals/:id/status` - Изменить статус
- `PUT /api/deals/:id/complete` - Завершить сделку
- `PUT /api/deals/:id/cancel` - Отменить сделку
- `DELETE /api/deals/:id` - Удалить сделку (только admin)

## 🗄️ Структура базы данных

Используется существующая таблица `deals`:
```sql
- id (SERIAL PRIMARY KEY)
- car_id (FK → cars)
- client_id (FK → clients)
- manager_id (FK → users)
- type ('sale', 'reservation')
- status ('new', 'in_process', 'completed', 'canceled')
- created_at (TIMESTAMP)
- completed_at (TIMESTAMP)
```

## 📁 Файловая структура

```
lib/features/deals/
├── domain/
│   ├── entities/
│   │   └── deal_entity.dart
│   └── repositories/
│       └── deals_repository.dart
├── data/
│   ├── models/
│   │   └── deal_model.dart
│   └── repositories/
│       └── deals_repository_impl.dart
└── presentation/
    ├── bloc/
    │   ├── deals_bloc.dart
    │   ├── deals_event.dart
    │   └── deals_state.dart
    ├── screens/
    │   └── deals_management_simple.dart
    └── widgets/
        ├── deal_card.dart
        ├── deal_form_dialog.dart
        └── deal_filters_dialog.dart
```

## ⚠️ Возможные проблемы

### Если не запускается Flutter
1. Проверьте, что все зависимости установлены: `flutter pub get`
2. Очистите кеш: `flutter clean && flutter pub get`
3. Перезапустите IDE

### Если не работает API
1. Убедитесь, что backend сервер запущен на порту 3000
2. Проверьте подключение к PostgreSQL
3. Проверьте правильность DATABASE_URL

### Если не отображаются сделки
1. Проверьте, что в базе данных есть тестовые данные
2. Убедитесь, что пользователь авторизован
3. Проверьте консоль браузера на ошибки

## 🎯 Следующие шаги

Система готова к использованию! Возможные улучшения:

1. **Загрузка данных** - автоматическая загрузка автомобилей и клиентов в формы
2. **Уведомления** - push-уведомления об изменениях статуса
3. **Отчеты** - аналитика по сделкам
4. **Мобильное приложение** - отдельное приложение для клиентов
5. **Интеграции** - подключение к внешним системам

## 📞 Поддержка

Если возникнут проблемы, проверьте:
1. Логи backend сервера
2. Консоль Flutter приложения
3. Network tab в браузере для API запросов

Система полностью интегрирована с существующей архитектурой и готова к продуктивному использованию!